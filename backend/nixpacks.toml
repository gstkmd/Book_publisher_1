[phases.setup]
nixPkgs = ["python311", "poetry"]

[phases.install]
cmds = ["poetry config virtualenvs.in-project true", "poetry install --no-root --no-interaction"]

[start]
cmd = ".venv/bin/uvicorn app.main:app --host 0.0.0.0 --port $PORT"
