[phases.setup]
nixPkgs = ["python311", "poetry"]

[phases.install]
cmds = ["python -m venv .venv", ". .venv/bin/activate", "poetry install --no-root --no-interaction"]

[start]
cmd = ". .venv/bin/activate && uvicorn app.main:app --host 0.0.0.0 --port $PORT"
