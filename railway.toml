[build]
builder = "nixpacks"

[deploy]
startCommand = "uvicorn app.main:app --host 0.0.0.0 --port $PORT"

[[services]]
name = "backend"
root = "backend"

[[services]]
name = "frontend"
root = "frontend"
